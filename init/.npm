#!/usr/bin/env bash

# list Packages that will be installed
node_modules=(
  bower
  express
  grunt-cli
  gulp
  jshint
  mean-cli
  mongoose
  nodemon
)

# install Node Packages
npm install -g  ${node_modules[@]}
